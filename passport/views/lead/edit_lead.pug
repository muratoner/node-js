include ../common/head
+head('Edit Lead')
.row
    form(action='/lead/edit/' + lead.id, method='POST', style="margin-top: 20px")
        .col-sm-12
            input(name='lead_email', type="email", placeholder="Enter you email address", value=lead.email).form-control
        .col-sm-12
            button(type="submit", style="margin-top: 20px").btn.btn-primary Save